# ENGINEERING_METRICS

Medición del rendimiento de la ejecución bajo **SOEC Engineering Methodology v1.0 (congelada)**. El objetivo NO es refinar el proceso, sino **medir si funciona**. Se actualiza al cerrar cada hito. Los datos deben ser honestos (estimados marcados como tales; desconocidos como `s/d`).

> Estado de la metodología: `ENGINEERING_METHODOLOGY_V1_0_FROZEN`. No se modifica durante M4-D y M5. La v1.1 sólo se evalúa tras esos hitos, por evidencia de estas métricas.

## Indicadores por hito

| # | Hito | Estado | Commits | Tests agregados | Interrupciones al usuario | Bugs por AUTOauditoría | Bugs por auditoría EXTERNA (usuario) | Rework (ciclos H) | Tiempo→consolidación |
|---|------|--------|--------:|----------------:|--------------------------:|-----------------------:|-------------------------------------:|-------------------|----------------------|
| 1 | Fundación M4 (M4-A→M4-C-C) | Fusionada (`main`, tag `fundacion-m4`) | 15 | ~991 acumulados (suite global) | Alta (auditoría por tramo, por diseño de gobernanza) | varios (referencias opacas, esConsumible, huella) | **muchos**: F-CB-1, F-CB-2, C-1..C-7, F-CBH-1, F-CCC-1/2, PR-1 | **3** (M4-C-A→A-H, M4-C-B→B-H, M4-C-C→correcciones) | por tramo |
| 2 | M4-D — andamiaje neutral + frontera + activación event-sourced | **Consolidado** (`verify` global 1026 verde) | 9 | 155 (2 paquetes M4); **1026 global** | **1** (feedback metodológico) | **2** autoauditoría (presupuesto fail-open; gate de nivel diferido) + DRY/consolidación (fnv1a, errorAborto) + no-filtración por path completo | 0 externo | 0 | 1 ciclo |
| 3 | M5 — Motor Estratégico Comercial (núcleo canónico + afirmación estratégica + aditivos descriptivos + decisión Empresa raíz) | **Cerrado** (`verify` global 1058 verde) | 3 | 32 (motor-estrategico + aditivos crm); **1058 global** | **0** (0 en implementación; 1 auditoría de cobertura solicitada por el usuario + 1 decisión de arquitectura reservada — no fricción evitable) | **2** autoauditoría (explicación honesta de retiro; rama muerta) | **auditoría de COBERTURA del usuario** → 5 aditivos descriptivos (Mercado/Competidor/BuyerPersona/PropVal/KPI) + resolución Empresa raíz | 0 (aditivos, no reapertura por defecto) | 1 ciclo + cierre de cobertura |
| 4 | M6 — Motor Creativo Estratégico (contexto-puente M5, gate AUTORITATIVO, dos fases con solicitudes canónicas+aprobación, vigencia gate único+materialización, concurrencia reparadora, replay frío, inmutabilidad runtime, lectura M7 por tupla completa) | **Cerrado** (`verify` global 1121 verde) | 6 | 61 (motor-creativo) + aditivos M3/event-store; **1121 global** | **4 (necesarias, de método)**: I-2 cierre prematuro; I-3 interacciones no demostradas; I-4 recuperación no demostrada en todas las fronteras; I-5 solicitudes/concurrencia-reparadora/lectura-por-tupla incompletas | discovery-first; autoauditoría de interacciones, recuperación y concurrencia reparadora (matriz de fallos + reintentos concurrentes; tupla pieza–variante–calendario) | 4 dictámenes de foco del usuario | 4 | 5 ciclos (núcleo→cierre→interacciones→recuperación→acotada) |

### Notas de honestidad
- **Fundación M4:** las "interrupciones" fueron mayormente **auditorías de gobernanza solicitadas por el usuario** (por tramo), no fragmentación artificial. El alto rework (los `-H`) es precisamente lo que la metodología v1.0 (autoauditoría adversarial + regla del 95%) busca **reducir**: muchas correcciones venían de escenarios adversariales ejecutables antes de detenerse.
- **M4-D neutral:** primer hito ejecutado ya con la metodología madura. Interrupciones ≈ 1 (el mismo usuario lo estimó: "hace unas semanas, 15; ahora, 1"). La autoauditoría (regla del 95%) detectó y corrigió el fail-open de presupuesto **antes** de cerrar → 0 rework externo hasta la fecha.
- **M5:** ejecutado sin interrupciones (0). El mayor valor del hito NO fue escribir código, sino **inspeccionar antes de construir**: el Bloque Maestro parecía greenfield, pero M2/M3 ya tenían 8 de los 11 modelos; construir un "motor estratégico" paralelo habría violado la regla SSOT del propio Bloque Maestro. La decisión de arquitectura (consolidar + construir solo el núcleo canónico de evaluabilidad que faltaba, en vez de duplicar) es el test del CTO aplicado. Autoauditoría corrigió 2 defectos (explicabilidad deshonesta en retiro; rama muerta) antes de cerrar.

## Incidencias de metodología (autorreportadas)

Registro honesto de detenciones/decisiones que NO cumplieron la metodología, para medir el desempeño (no para cambiar el proceso).

| # | Hito | Incidencia | Detección | Corrección |
|---|------|-----------|-----------|------------|
| I-1 | M4-D neutral | Se **difirió** el cableado del gate de nivel de activación clasificándolo como "dependiente de gobernanza/ratificación", cuando en realidad era cableable como **opción inyectada** (neutral, patrón del presupuesto) → una fila del test de independencia que respondía "No" se recomendó en vez de ejecutarse. | Auto (al aplicar con rigor la Directiva de Autonomía por Hito). | Implementado el gate `ACTIVACION` inyectado + tests en el mismo ciclo; sin rework externo. |
| I-2 | M6 | **Cierre prematuro del hito:** se entregó el núcleo + una lista de "pendientes aditivos" pidiendo autorización, cuando esos pendientes (brief/pieza ampliados, orquestación end-to-end, obsolescencia automática) eran **criterios LOCKED** del Bloque Maestro, no deuda opcional. Detenerse contradecía el hito ya autorizado. | Externa (usuario: `AUDITORIA_M6_REQUIERE_CIERRE_INTERNO`). | Se agotó M6 completo sobre la misma rama sin nueva detención: brief/pieza ampliados aditivamente, pipeline conectado, obsolescencia autoritativa, contratos M7 completos; `verify` 1093 verde. |
| I-3 | M6 | **Componentes sin INTERACCIONES demostradas:** el cierre probaba cada pieza (A/B, calendario, validador) por separado y afirmaba "obsolescencia automática", pero no demostraba la cadena gobernada (aprobación→vigencia→calendario), ni obsolescencia con autoridad única, ni fallo parcial reparable, ni replay frío. "Reutilizar componentes" ≠ "probar la composición". | Externa (usuario: `AUDITORIA_M6_REQUIERE_CORRECCIONES_FOCALIZADAS`). | Vigencia como gate único con materialización idempotente; pipeline en dos fases con aprobación humana por versión exacta; tests de fallo parcial reparable, replay frío y `listarPiezasAprobadas`; `verify` 1095 verde. |
| I-4 | M6 | **Recuperación no demostrada en todas las fronteras:** el cierre probaba una sola frontera de fallo parcial y un replay "frío" sobre el mismo objeto de store; faltaba la matriz por frontera con conteo de eventos, replay desde un store nuevo, e inmutabilidad runtime. | Externa (`AUDITORIA_M6_REQUIERE_CORRECCIONES_FOCALIZADAS_2`). | Matriz de fallos por frontera (conteo=1); `exportar/desdeInstantanea` (store nuevo desde log serializado); `congelarProfundo`; `verify` 1112 verde. |
| I-5 | M6 | **Cierres finales acotados incompletos:** la solicitud canónica cubría solo la pieza (faltaban variante y estrategia-por-política); la concurrencia reparadora no se probaba tras fallo parcial en los cuatro bordes; la lectura M7 validaba artefactos aislados, no la tupla pieza–variante–calendario. | Externa (`AUDITORIA_M6_REQUIERE_CORRECCION_FINAL_ACOTADA`). | Solicitudes por artefacto (pieza/variante/estrategia por política); `componer` idempotente total ⇒ reintentos concurrentes convergen (conteo=1); `listarPiezasAprobadas` exige variantes aprobadas y `calendarizar` rechaza combinación cruzada; `verify` 1121 verde. |
| I-6 | M7 | **Cierre prematuro (núcleo):** se declaró "núcleo consolidado" con criterios LOCKED (sandbox M4 real, retry/backoff, PAUSA, reconciliación exhaustiva, matriz de 30) tratados como deuda no bloqueante. | Externa (`AUDITORIA_M7_REQUIERE_CIERRE_INTERNO`). | Se cerraron sandbox M4 real, idempotencia lógica+CONFLICTO y PAUSA sobre la misma rama; adenda al ADR-0030. |
| I-7 | M7 | **Segundo cierre prematuro:** se entregó otro informe parcial ("continúo en las próximas rondas" — frase prohibida) con el ciclo presupuestario, la compensación de primera clase, el retry canónico, el reconciliador exhaustivo, la matriz de 30 y el endurecimiento M8 aún pendientes. | Externa (`DIRECTIVA — CONTINUAR M7 HASTA CIERRE TOTAL`). | Se agotó M7 en una sola pasada continua: ciclo de reserva event-sourced, compensación agregada, `decidirRetry`+backoff con re-validación de gates, reconciliador con reservas huérfanas, clasificación semántica M8, matriz de 30 → tests, fix de FSM latente; `verify` 1163 verde. |
| I-9 | M8 | **Cierre incompleto por sub-especificación de las matrices:** el núcleo era correcto pero (1) sólo 7 de 12 fronteras de fallo parcial, (2) reconciliador no demostrado clase por clase, (3) consolidación entre experimentos sin agregado ni pruebas, (4) obsolescencia sin matriz de causas. | Externa (`AUDITORIA_M8_REQUIERE_CIERRE_FOCALIZADO`). | Se DESCOMPUSO la evaluación en pasos event-sourced (habilita 12 fronteras reales); reconciliador a 12 clases con test por clase; `ConsolidacionOperacion` event-sourced + 14 tests (compatibles/incompatibles/contradicción/dedup/cross-tenant/concurrencia/replay); matriz de obsolescencia de 10 causas + invariantes (aprendizaje no vigente, memoria conserva histórico, nueva versión no sobrescribe, idempotente/concurrente); M9 endurecido (freeze profundo de anidados, cross-tenant). 58→95 tests; `verify` 1300 verde. **Lección:** las matrices deben ser EJECUTABLES y completas por enumeración; descomponer para que cada frontera sea observable. |
| I-8 | M7 | **Sobreafirmación de cobertura:** el informe de cierre afirmaba "reconciliador exhaustivo" (solo 4 clases enumeradas), matriz de 30 y de fronteras "demostradas" sin citar tests exactos ni aserciones, y gates de retry no probados individualmente. La dirección arquitectónica era correcta; la EVIDENCIA no acreditaba lo prometido. | Externa (`AUDITORIA_M7_REQUIERE_REAUDITORIA_Y_CIERRE_FOCALIZADO`). | Cobertura demostrada y completada: matriz EJECUTABLE del reconciliador (12+ clases, cada una con test), 18 fronteras con conteos, 30 escenarios con nombre de test exacto, 10 gates re-evaluados por intento, contratos M8 explícitos; 5 correcciones de correctitud surgidas de las matrices (reconciliación forward, consumo idempotente por rid, presupuesto sin doble conteo, encolar robusto, evidencia idempotente). 42→84 tests; `verify` 1205 verde. **Lección:** un informe no debe declarar cobertura sin trazabilidad escenario→test→aserción; construir la matriz EJECUTABLE, no solo documentada. |

| # | Macrobloque | Estado | Ciclos auditoría | Tests | Incidencias | Método | Origen incidencias | Correcciones | Iteraciones |
|---|---|---|---|---|---|---|---|---|---|
| PCE-1 | Producto Autónomo SOEC (Productization & Customer Experience) — cambio de etapa: convierte M5–M9 en la experiencia de un **Director de Marketing Autónomo**. Deliverables: leyes permanentes registradas (LEY 1 / Reducción / Excepción), auditoría de fricción de `apps/web` (12 módulos → conversación), ADR-0033 de arquitectura UX (HOME / Bandeja de Decisiones / Centro de Explicaciones / Onboarding mínimo / Modo Autónomo / Objetivos / Políticas, cada superficie mapeada a su puerto de lectura M5–M9), y **prototipo viewable** de alta fidelidad (`docs/prototipos/pce-1-soec-director.html`, artefacto publicado). | **Diseño cerrado** (arquitectura de producto + prototipo); implementación productiva de `apps/web` sobre M5–M9 = tramo de ingeniería siguiente | — | 0 código (bloque de producto/UX); ADR-0033 + prototipo | 0 | discovery-first (auditó `apps/web`, 18 rutas / 12 módulos); reutiliza los 4 puertos de lectura + autonomía M4-D; NINGÚN motor nuevo; autoauditoría de fricción (corregida en el diseño: se eliminaron reportes/medición/config de segmentos/hipótesis) | LEY 1 y principios de Reducción/Excepción | — | 1 pasada |
| 7 | M9 — Motor de Optimización Continua Gobernada (`@soec/motor-optimizacion`: CicloOptimizacion 11 estados event-sourced, oportunidades/alternativas tipadas, motor de comparación puro explicable, counterfactual, NO_ACTUAR de 1.ª clase, PropuestaOptimizacion versionada con aprobación humana canónica + aplicación SIMULADA creando nuevas versiones, guardas de oscilación, memoria de decisiones event-sourced, reconciliador 16 clases, LecturaCicloSOEC global inmutable) — **CIERRA el ciclo funcional M5→M6→M7→M8→M9 en modo SIMULADO, con dos iteraciones** | **Cerrado interno** (`verify` global 1379 verde), pendiente de auditoría externa | 0 (aún) | 79 (`@soec/motor-optimizacion`, 5 archivos); **1379 global** | 0 externas; 3 fixes de correctitud propios | discovery-first (reuso de los 4 puertos de lectura M5–M8 + aprobación canónica + presupuesto + escrituras canónicas vía puerto AplicadorCambios; NO se creó máquina paralela); 40 escenarios con test exacto; 18 fronteras de fallo parcial; reconciliador 16 clases; bucle cerrado con **dos iteraciones**; replay frío integral; una recomendación ≠ orden; aprobación humana obligatoria; aplicar = nuevas versiones | aplicación de lecciones I-6..I-9 desde el inicio | 3 (aplicarSimulado idempotente; orden de proponer/índice; revalidación por versión) | 1 pasada continua |
| 6 | M8 — Motor de Medición, Aprendizaje y Conocimiento Operacional (`@soec/motor-medicion`: ObservacionOperacion event-sourced, EvaluacionOperacion DESCOMPUESTA en pasos event-sourced, evaluación de resultado/hipótesis puras reusando calidad de @soec/medicion y epistemología de M5, atribución cauta sin causalidad, aprendizaje canónico reusando @soec/aprendizaje, ConsolidacionOperacion multi-experimento, MemoriaService event-sourced, reconciliador 12 clases, contratos M9 inmutables) | **Cerrado interno** (`verify` global 1300 verde), pendiente de auditoría externa | 1 (cierre focalizado) | 95 (`@soec/motor-medicion`, 8 archivos); **1300 global** | 1 externa (I-9); 5 fixes de correctitud propios | discovery-first (reuso LecturaOperativa M7 + @soec/medicion + @soec/aprendizaje + LecturaConocimiento M5); 30 escenarios con test exacto; **12 fronteras** de fallo parcial con conteos; matriz del reconciliador **12 clases**; **consolidación canónica** (compatibles/incompatibles/contradicción/dedup/replay); **matriz de obsolescencia** (10 causas); M9 endurecido (freeze profundo, cross-tenant); ausencia≠cero; correlación≠causalidad | aplicación I-6..I-8 + dictamen I-9 | 5 (descomposición ES de evaluación; consumo+ejecución ambas clases; memoria ES; FSM VALIDADA→DESCARTADA; índice reparable) | 2 (núcleo + cierre focalizado) |

| 5 | M7 — Motor de Operación y Ejecución Gobernada (OrdenEjecucion 11 estados, validación autoritativa M6, scheduler, cola con lease, ejecutor gobernado idempotente + sandbox M4 real + presupuesto M4-D, ciclo de reserva event-sourced, compensación de primera clase, retry canónico con re-evaluación de gates por intento, reconciliador exhaustivo 12+ clases, evidencia inmutable, contratos M8 con clasificación semántica) | **Cerrado** (`verify` global 1205 verde) | 1 | 84 (`@soec/motor-operacion`, 6 archivos); **1205 global** | **3 (necesarias, de método)**: I-6 cierre prematuro ("núcleo consolidado"); I-7 segundo cierre prematuro ("próximas rondas"); I-8 sobreafirmación de cobertura (matrices no demostradas con tests exactos) | discovery-first; matriz EJECUTABLE del reconciliador (12+ clases); matriz de 18 fallos parciales por frontera con conteos de eventos/versiones; matriz de 30 escenarios con test exacto; re-evaluación de 10 gates por intento; replay frío integral; **fixes de correctitud** surgidos de las matrices (FSM EN_EJECUCION→EXPIRADA; reconciliación forward del efecto aplicado; consumo idempotente por rid; presupuesto sin doble conteo en reintento; encolar robusto a desincronización) | 3 dictámenes de foco del usuario | 3 | 4 ciclos (núcleo→cierre interno→cierre total→reauditoría) |

## Tendencia buscada (hipótesis a validar con M4-D y M5)
- ↓ Interrupciones por hito.
- ↓ Bugs encontrados por auditoría EXTERNA (↑ proporción encontrada por autoauditoría).
- ↓ Rework (ciclos `-H` posteriores a auditoría externa).
- Cobertura de regresión estable o creciente; sin regresiones.

## Definiciones
- **Interrupción:** detención que devuelve el control al usuario fuera de las 14 causas legítimas (estrategia/gobernanza/credenciales/SDK/datos/costo/irreversible/ambiental/pedido). Las detenciones por decisión estratégica **no** cuentan como fricción evitable.
- **Bug por autoauditoría:** defecto que Claude encontró y corrigió por sí mismo antes de cerrar el hito.
- **Bug por auditoría externa:** defecto que sólo apareció en la revisión del usuario (idealmente → 0).
- **Rework:** hitos/tramos que hubo que reabrir (`-H`) por hallazgos que podrían haberse anticipado.

## Método de actualización
Al cerrar cada hito, agregar una fila con datos objetivos (git log para commits, suites para tests, conteo real de detenciones). No estimar donde exista dato objetivo; marcar `s/d` lo desconocido. Revisión de la metodología: **sólo** al terminar M4-D y M5.
