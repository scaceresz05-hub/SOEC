# Copiar a .env para desarrollo local. NUNCA versionar .env real.
# PostgreSQL local (docker compose -p soec, puerto 5544 aislado de otros proyectos).
DATABASE_URL=postgres://soec:soec@localhost:5544/soec
PORT=3000
